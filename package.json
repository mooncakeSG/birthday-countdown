{
  "name": "birthday-countdown",
  "version": "1.0.0",
  "description": "Birthday countdown app with React frontend",
  "scripts": {
    "build": "cd birthday-countdown-react && npm install && npm run build",
    "start": "cd birthday-countdown-react && npm start",
    "dev": "cd birthday-countdown-react && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
